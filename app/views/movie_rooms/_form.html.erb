<%= form_with(model: movie_room, local: true) do |form| %>
  <% if movie_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_room.errors.count, "error") %> prohibited this movie_room from being saved:</h2>

      <ul>
        <% movie_room.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
